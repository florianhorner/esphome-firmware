substitutions:
  name: !secret device_3_name
  friendly_name: !secret device_3_friendly_name
  static_ip: !secret device_3_ip
  api_encryption_key: !secret device_3_api_key
  manifest_path: !secret device_3_manifest_path
  gateway: !secret gateway
  subnet: !secret subnet

packages:
  base: !include common/ble-proxy-base.yaml
